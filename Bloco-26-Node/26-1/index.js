const readline = require('readline-sync');

function executaCalculo() {
  const altura = readline.questionFloat("digita a Alutura:").toFixed(2);
  const peso = readline.questionFloat("digite o peso:").toFixed(2);
}
